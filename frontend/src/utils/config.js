// Config values for the entire application. Ensure to document each constant
//Within each config we have treeConfig, and then some other config; each subcategory is related to the component that uses it
export const Config = Object.freeze({
  MAX_DEPTH: 2,
  FADE_OPACITY: 0.25,
  NO_DEPTH_LIMIT: -1,
});
